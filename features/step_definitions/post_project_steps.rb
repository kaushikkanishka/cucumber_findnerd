

Then(/^I should see the following on Post Project page$/) do |table|
  # table is a table.hashes.keys # => [:Hire a freelancer or Tech team!]
  pending
end

And(/^"([^"]*)" option should be selected by default in "([^"]*)" dropdown$/) do |arg1, arg2|
  pending
end

And(/^I should see the following dropdown on selecting "([^"]*)" radio button$/) do |arg, table|
  # table is a table.hashes.keys # => [:Budget]
  pending
end

And(/^I should see the following under "([^"]*)"$/) do |arg, table|
  # table is a table.hashes.keys # => [:Our team with No. of developers]
  pending
end